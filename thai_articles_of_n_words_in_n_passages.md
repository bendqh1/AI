Create for me an article in Thai about construction.

The article should include about 50 words organized in about 5 passages.

Each passage should be separated from another passage by an empty line (line break) that can be copied to the clipboard alongside with the text.

## Notes

* Put white spaces between words as it would lower the chance for mistakes when doing word count with Libre Office Writer.
* If the article would include numbers, then the article should not include Thai numbers, but rather, English numbers only.
* The article shouldn't be split into list items (`<li>`) inside an ordered list (`<ol>`) because I can't copy the list item numbers from the output into clipboard.
